## Road Accident Severity Classification (ML + NLP)

This project predicts whether a traffic accident resulted in **no injury**, **minor injury**, or **major/fatal injury** using both structured data (weather, visibility, location, time) and unstructured text (crash descriptions).
It demonstrates an **end-to-end machine-learning pipeline** integrating tabular data, natural-language processing, and model evaluation.

